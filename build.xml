<?xml version="1.0"?>
<project name="AppCentral Server" basedir="source" default="package">

	<property name="name" value="appcentral-server" />
	<property name="version" value="2.4.0" />

	<target name="sources">
		<tar destfile="../build/${name}-src-${version}.tgz" compression="gzip">
			<tarfileset dir="." prefix="${name}-src-${version}">
				<exclude name="**/.svn/**" />
				<exclude name="project.index" />
				<exclude name=".project" />
				<exclude name=".settings/**" />
				<exclude name=".cache/**" />
			</tarfileset>
		</tar>
	</target>

	<target name="package">
		<tar destfile="../build/${name}-${version}.tgz" compression="gzip">
			<tarfileset dir="." prefix="${name}-${version}">
				<exclude name="TODO.txt" />
				<exclude name="build/**" />
				<exclude name="nbproject/**" />
				<exclude name="project.index" />
				<exclude name=".project" />
				<exclude name=".settings/**" />
				<exclude name=".cache/**" />
				<exclude name="build.xml" />
			</tarfileset>
		</tar>
	</target>

</project>
